{
  "compilerOptions": {
    /* Language and Environment */
    "target": "ES2021",                                 /* Set the JavaScript language version for emitted JavaScript. */
    "module": "ESNext",                                 /* Use ES modules, which are modern and work well in browsers. */
    
    /* Modules */
    "esModuleInterop": true,                            /* Eases support for importing CommonJS modules in ES modules. */
    "forceConsistentCasingInFileNames": true,           /* Ensure consistent casing in imports. */

    /* Emit */
    "outDir": "public/assets/js",                       /* Output directory for compiled JavaScript files. */

    /* Type Checking */
    "strict": true,                                     /* Enable strict type-checking options. */
    "skipLibCheck": true                                /* Skip type checking all .d.ts files to improve performance. */
  },
  "include": ["src/**/*"],                              /* Include all TypeScript files in the src folder. */
  "exclude": ["node_modules"]                           /* Exclude node_modules folder. */
}
